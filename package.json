{
  "dependencies": {
    "kaboom": "^3000.0.0-beta.0"
  },
  "scripts": {
    "build": "esbuild index.js --bundle > build/build.js",
    "dev": "npm run build && npx http-server ./build"
  },
  "devDependencies": {
    "esbuild": "^0.17.5",
    "http-server": "^14.1.1"
  }
}
